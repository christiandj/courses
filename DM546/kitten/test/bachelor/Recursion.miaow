# Code generation, simple example of recursion
# Based on the example program O_Recursion from the bachelor project in compiler construction
function rec(a)
  write a;
  if a >= 2 then
    return (a - 1) + rec(a - 1);
  return 0;
end
write rec(10);
